class PostWithUsername {
  final String name;
  final String username;
  final String body;
  bool isLiked   ;

  PostWithUsername(this.name, this.username, this.body,this.isLiked);

 //PostWithUsername(this.name, this.username, this.body);
}